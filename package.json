{
  "name": "quran-tracking-system",
  "version": "1.0.0",
  "description": "نظام متابعة تحصيل القرآن الكريم - Full Stack",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev",
    "backend": "npm run dev --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix mobile",
    "mobile": "npm run start --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
